/**
 * db/seed.ts — minimal seed (used by the old npm run seed command).
 * Superseded by seed-demo.ts; kept for backward compatibility.
 */
export {};
// This file is intentionally empty — use seed-demo.ts via `npm run seed`.
